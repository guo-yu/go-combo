package combo

import "os"

import (
  "io",
  "io/ioutil",
  "fmt",
  "time",
  "strings",
  "os"
)

import . "github.com/visionmedia/go-debug"

const VERSION = "0.1.0"

var debug = Debug("combo")

func Fromfiles(argvs, ...interface{}) error {
  
}

func splitParams(params string) () {
  
}
